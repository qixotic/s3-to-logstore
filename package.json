{
  "name": "s3-to-logstore",
  "version": "0.0.1",
  "description": "Creates an AWS lambda function for parsing S3 / CloudFront / Cloudtrail logs stored on S3 and pushes those events into your log storage of choice via Winston.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Linus Wong",
  "license": "MIT",
  "dependencies": {
    "split": "^1.0.0",
    "winston": "^2.2.0",
    "winston-papertrail": "^1.0.2"
  }
}
